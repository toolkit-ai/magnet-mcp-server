# Sample environment configuration for local development
# Copy this file to .env and fill in your values

# Required: Your Magnet API key
# Get this from https://magnet.run/settings (or your local instance)
MAGNET_API_KEY=your-api-key-here

# Optional: Override the Magnet API base URL
# Use this when running against a local Magnet development server
# Defaults to http://magnet.run if not set
MAGNET_WEB_API_BASE_URL=http://localhost:3000
